{
  "name": "mcp-server",
  "version": "1.0.0",
  "mcpServers": {
    "nodeServer": {
      "command": "node",
      "args": ["${extensionPath}${/}example.ts"],
      "description": "Runs my example mcp-server"
    },
    "kappaServer": {
      "command": "node",
      "args": ["${extensionPath}${/}example.ts"],
      "description": "Calls a super intelligent AI agent to make your life easier"
    }
  }
}
